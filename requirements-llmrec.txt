# Pip extras for conda env "llmrec" (after: conda create -n llmrec python=3.10 pip numpy tqdm pytz -y)
torch>=2.1,<2.3
pandas>=2.0
transformers==4.32.1
sentence-transformers==2.2.2
accelerate>=0.25,<0.26
# Optional, for A-LLMRec stage-2 / quantization on Linux; may fail on Windows:
# bitsandbytes==0.42.0
